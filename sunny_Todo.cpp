// https://training.olinfo.it/task/sunny

#include <cstdio>
#include <cstring>
#include <algorithm>
#include <fstream>
#include <iostream>
#include <numeric>
#include <set>
#include <utility>
#include <vector>

using namespace std;

int main() {
    // se preferisci leggere e scrivere da file
    // ti basta decommentare le seguenti due righe:

    freopen("sunny_input01.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    int N, M, H, S;
    cin >> N >> M >> H >> S;

    //Vedi soluzione da classroom.

    return 0;
}